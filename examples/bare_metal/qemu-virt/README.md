# AArch64 Bare-Metal PL011 UART Example

A minimal, zero-allocation bare-metal example demonstrating how to stream formatted diagnostics 
directly to the ARM PL011 UART on QEMU's virt machine using `jplcz_microfmt`.

## Building and Running

```bash
make
make run
```
